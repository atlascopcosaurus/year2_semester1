<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Parsed Sentence Result</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
</head>

<body>
    <div class="container mt-5">
        <h1 class="mb-4">Parsed Sentence Result</h1>

        <div class="card p-4 mb-4">
            <h4>Original Sentence:</h4>
            <p class="fs-4"><strong>
                    <%= originalSentence %>
                </strong></p>
        </div>

        <div class="card p-4">
            <h4>POS Tags, Lemma, and NER:</h4>
            <ul class="list-group">
                <% results.forEach(result=> { %>
                    <li class="list-group-item">
                        <strong>Word:</strong>
                        <%= result.word %><br>
                            <strong>Part of Speech:</strong>
                            <%= getPosDescription(result.pos) %> (<%= result.pos %>)<br>
                                    <strong>Lemma:</strong>
                                    <%= result.lemma %><br>
                                        <strong>Named Entity Recognition (NER):</strong>
                                        <%= result.ner==='O' ? 'Not a recognized entity' : result.ner %>
                    </li>
                    <% }) %>
            </ul>
        </div>

        <a href="/" class="btn btn-primary mt-3">Parse another sentence</a>
    </div>
</body>

</html>